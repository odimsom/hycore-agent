# HyCore Agent Configuration
# Hytale Dedicated Server Management

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=*

# Default Hytale Server Settings
DEFAULT_HYTALE_PORT=5520
DEFAULT_JVM_XMS=2G
DEFAULT_JVM_XMX=4G

# Rutas por defecto (gestionadas automáticamente por el agente)
# Los archivos de Hytale se copian a: ./hytale-files/
# Los servidores se crean en: ./servers/
# Los backups se guardan en: ./backups/

# Si quieres rutas personalizadas, descomenta y modifica:
# CUSTOM_HYTALE_FILES_PATH=/ruta/personalizada/hytale-files
# CUSTOM_SERVERS_PATH=/ruta/personalizada/servers
# CUSTOM_BACKUPS_PATH=/ruta/personalizada/backups

# Security
# API_KEY=your-secure-api-key

# Logging
LOG_LEVEL=info
LOG_TO_FILE=true
LOG_FILE_PATH=./logs